/* eslint-disable */
import React from 'react';

const Beta = ({ children }) => (
  <span>
    {' '}
    {children}
    <sup>
      <small>&nbsp;Beta</small>
    </sup>
  </span>
);
export default Beta;
